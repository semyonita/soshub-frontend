# Material UI - SOSHub task

***Get project be runned***
***
To build the project, install it and run:

```bash
npm install --legacy-peer-deps
npm run dev
```
***
You should also setup .env file
```bash
cp .env.example .env
```
And here you are